[project]
name = "sql-repair-agent"
version = "0.1.0"
description = "SQL Repair Agent for OpenEnv Hackathon"
requires-python = ">=3.9"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "requests",
    "openai"
]